---
tags:
  - Education/Programming
  - Education/Programming/Python
  - Education
---
--- 
12/26/24

---
Data types: strings, integers, floats, boolean

Integer = whole number - Number but not quotes.
Commas in large number can be replace with underscores.
Float has a decimal in it. 
Boolean = True or False
type(whatever) to get the type.

type conversion = type casting
int()
str()
float()

Mathematic operations:
```(+, -, * , /, )```
Plus, minus, multiplication, division
// can be used for division but will strip off any decimals and only leave the whole number. Not always what you want. 
%% ** %% = Exponent
PEMDAS ( Parentheses, Exponents, Multiplication, Division, Addition, Subtraction) - Left to right so if two have same importance, left to right. 

round() = Rounds up or down based on number and gives that. 
round(item, number) gives number of decimal places. 

Assignment operator, +=, -=, /=, *= will evaluate the numbers and return the results. For example" += 2 Score += 2 will add two and update the variable. 

F-string (f"Whatever and add var {var })
