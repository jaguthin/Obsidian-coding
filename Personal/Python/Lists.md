---
tags:
  - Education/Programming
  - Education/Programming/Python
  - Education
---

Can split .split(',') == Divider specified if not a space.
.append to add to a list. 
.index('34') = Will look for item in list and create an error if not found.

if 'whatever' in list_var: - Variable needs to be stated.

len(var) - Give length of list. 

.sort() = Duh? It sorts.
sorted - sorts but does not modify list ( reverse=True)
sorted(list_name,reverse=True)

.pop or .pop(index) will remove items from list. Removes last item from list by default. 

.clear() will clear a list.

.strip() can be used to remove white space.
.lstrip() - All leading space.
.rstrip() - All trailing space.
