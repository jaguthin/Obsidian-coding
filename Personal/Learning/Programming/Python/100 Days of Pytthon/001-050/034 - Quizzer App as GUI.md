---

import html
html.escape will fix escape issues.

One can set the datatype of a variable.

like:
age: int

Can also do that in a function
-> datatype at start of function so it says what type is to be returned.
